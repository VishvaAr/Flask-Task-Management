# Flask-Task-Management


# Flask Task Management API

A lightweight, RESTful API built with Python and the Flask micro-framework. This project demonstrates the implementation of CRUD (Create, Read, Update, Delete) operations, database management with SQLAlchemy, and professional API design patterns.

## 🚀 Features

* **Full CRUD Operations**: Create, view, and delete tasks.
* **Persistent Storage**: Integrated with SQLite using SQLAlchemy ORM.
* **RESTful Standards**: Implementation of proper HTTP methods (GET, POST, DELETE) and status codes.
* **Input Validation**: Server-side checks for data integrity.

## 🛠️ Tech Stack

* **Language**: Python 3
* **Framework**: Flask
* **Database**: SQLite
* **ORM**: Flask-SQLAlchemy

## 📂 Project Structure

```text
flask-todo-api/
├── venv/             # Virtual environment
├── api.py            # Main application entry point & routes
├── db.sqlite         # SQLite database file
├── .gitignore        # Files to exclude from Git
└── requirements.txt  # Project dependencies
